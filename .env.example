# Copy to .env to run the live-provider tests (they skip when a key is absent).
TYPESAFE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Set to 1 to opt in to tests that hit real provider APIs.
DECISION_ENGINE_LIVE=
